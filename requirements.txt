# ============================================================================
# astrbot_plugin_Xagent_searcher 核心依赖清单
# ============================================================================
# 规范文档参考：第 1 章 - 项目基础定义与依赖矩阵
#
# 本插件采用极致的异步化与低耦合原则，所有版本均选择最新稳定版本。
# 遵循"用新不用旧"原则，拒绝使用老旧依赖库。
# ============================================================================

# 核心异步网络库（HTTP/2、代理隧道、连接池管理）
# 替代同步的 requests 库，是防止事件循环阻塞的核心支柱
# 规范文档参考：第 1 章、第 3 章（异步网络基建与事件循环保护）
httpx[http2]>=0.26.0

# 图像处理库（像素级缩放、JPEG 品质压缩、动态压缩算法）
# 唯一合法的图像处理底层驱动，所有 PIL CPU 密集型任务需通过 asyncio.to_thread() 委派
# 规范文档参考：第 1 章、第 3 章（大文件拦截与动态压缩流水线）
Pillow>=10.1.0

# 数据验证库（强制校验 X API 复杂嵌套结构）
# 从 Pydantic v2 开始，性能大幅提升，支持异步验证
# 规范文档参考：第 1 章、第 2 章（关系型数据水合与扩展字段映射）
pydantic>=2.5.0

# ============================================================================
# 注意事项
# ============================================================================
# 1. astrbot 框架提供的依赖（如 asyncio, logging 等标准库）无需重复声明
# 2. astrbot.api 提供的 Logger 和上下文管理已集成在框架中
# 3. 所有版本采用 >= 最小版本的方式，允许 pip 自动升级到兼容的最新版本
# 4. httpx[http2] 包含 HTTP/2 支持，用于高性能多媒体下载并发
# 5. 推荐在生产环境中使用 pip install -r requirements.txt --upgrade
# ============================================================================

